%h1= titulo

= render partial: 'cartas/form', locals: { version: @version }

= link_to 'Mostrar', [@carta, @version], class: 'btn'
= link_to 'Otras versiones', carta_versiones_path(@carta), class: 'btn'
