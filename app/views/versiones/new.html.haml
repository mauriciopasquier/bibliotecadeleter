%h1= titulo

= render partial: 'cartas/form', locals: { version: nueva_version,
  bloqueados: [:nombre] }

.row-fluid.botones
  .span12
    = link_to 'Cartas', cartas_path, class: 'btn'
    = link_to 'Otras versiones', @carta, class: 'btn'
