- cache torneo do
  .row-fluid
    .fecha.span4
      - if can? :read, torneo
        = link_to_mostrar torneo, torneo.fecha
      - else
        = torneo.fecha
    .tienda.span4= torneo.tienda_nombre
    .formato.span4= torneo.formato_nombre
