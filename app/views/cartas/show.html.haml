.row-fluid
  .span6= render partial: 'layouts/galeria',
    locals: { imagenes: @carta.canonica.imagenes, estilo: :original }
  .span6= render @carta

- if @carta.versiones.size > 1
  .row-fluid
    .span12= link_to 'Todas las versiones', carta_versiones_path(@carta), class: 'btn'
