.row-fluid{class: cycle('even', 'odd')}
  .nombre.span4
    - if can? :read, carta
      = link_to_mostrar carta, carta.nombre
    - else
      = carta.nombre
  .coste.span1= carta.canonica.coste
  .tipo.span3= carta.canonica.linea_de_tipos
  .expansiones.span4= carta.lista_de_expansiones
