%h1= titulo

= render partial: 'form', locals: { version: @carta.canonica }

.row-fluid.botones
  .span12
    = link_to 'Mostrar', @carta, class: 'btn'
    = link_to 'Cartas', cartas_path, class: 'btn'
