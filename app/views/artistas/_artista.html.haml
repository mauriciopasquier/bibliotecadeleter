- cache artista do
  .row-fluid
    .nombre.span8
      - if can? :read, artista
        = link_to_mostrar artista, artista.nombre
      - else
        = artista.nombre
    .cantidad.span4.hidden-phone= artista.cantidad
