= form_for [forem, :admin, @group], html: { class: 'form group' } do |f|
  .row-fluid
    .span12
      = f.label :name
      = f.text_field :name, class: 'span12', required: true, autofocus: true
  .row-fluid.botones
    .span12
      = f.submit class: "btn btn-success"
