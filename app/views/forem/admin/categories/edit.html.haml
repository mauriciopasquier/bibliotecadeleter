%h1= t("forem.admin.category.edit", title: @category.name)

= render "form"

.row-fluid.botones
  .span12
    = link_to t('back_to_admin', scope: "forem.admin"), forem.admin_root_path,
      class: 'btn'
