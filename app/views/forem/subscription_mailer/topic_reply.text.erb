<%= @user.nick %>:

<%= I18n.t('received_reply', scope: 'forem.topic') %> Podés ver el tema acá:

  <%= forem.forum_topic_url(@post.topic.forum_id, @post.topic) %>

Para ignorar esta cacofonía de voces incomprensibles, seguí este link:

  <%= forem.unsubscribe_forum_topic_url(@post.topic.forum, @post.topic) %>
